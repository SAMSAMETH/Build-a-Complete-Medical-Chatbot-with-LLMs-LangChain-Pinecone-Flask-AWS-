# Core tools (pinned for stability)
pip==23.3.2
setuptools==70.0.0
wheel==0.45.1

# Web framework
flask==2.3.3
gunicorn==21.2.0
python-dotenv==1.0.1

# LangChain ecosystem (slightly older but more stable versions)
langchain==0.0.346
langchain-community==0.0.29
langchain-pinecone==0.0.4
langchain-huggingface==0.0.2

# Vector DB
pinecone-client==3.2.2

# Embeddings + Transformers (lighter versions)
sentence-transformers==2.2.2
transformers==4.30.2
tokenizers==0.13.3
huggingface-hub==0.16.4

# PDF parsing (lighter alternatives)
pypdf==3.17.0  # Lighter than 5.x
scikit-learn==1.3.2  # Lighter version

# Additional optimizations for memory
psutil==5.9.5  # For memory monitoring
protobuf==3.20.3  # Specific version to avoid conflicts
torch==2.0.1  # Lighter with CPU-only support
numpy==1.24.3  # Stable version